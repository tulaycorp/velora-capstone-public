[project]
name = "velora-backend"
version = "0.1.0"
description = "FastAPI backend scaffold for Velora."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
